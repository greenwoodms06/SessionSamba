{
  "name": "sessionsamba",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A portable conference-schedule standard and an offline-first PWA that consumes it.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test 'tests/**/*.test.js' && vitest run",
    "import:siggraph": "python3 scripts/import_siggraph2026.py",
    "test:lib": "node --test 'tests/**/*.test.js'",
    "test:ui": "vitest run",
    "test:e2e": "node e2e/run.mjs",
    "shots": "node e2e/shots.mjs"
  },
  "dependencies": {
    "@fontsource-variable/instrument-sans": "^5.3.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "playwright": "^1.61.1",
    "vite": "^8.1.5",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.10"
  }
}
